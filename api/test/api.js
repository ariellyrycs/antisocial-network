/**
 * Created by arobles on 10/14/14.
 */
var chai = chai || require('chai');
    //expect = chai.expect,
    //controllers = require('./../controllers/pets.js');
describe('post method', function () {
    'use strict';

    it('must be equal to be reliable', function (done) {
        //console.log(controllers.findAllPets());
        //expect({a:0}).to.deep.equal(controllers);
        done();
    });
});